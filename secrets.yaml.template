# secrets.yaml — valeurs locales pour le développement
# Ce fichier est GITIGNORE — ne jamais committer.
# Copier ce fichier : cp secrets.yaml.template secrets.yaml
# Remplir les valeurs ci-dessous.

adapters:
  mongodb:
    uri: mongodb://localhost:5971   # URI de connexion MongoDB locale

lm:
  planner:
    api_key: sk-ant-XXXX           # Clé API Anthropic (ou "lm-studio" pour LLM local)

